from setuptools import setup

setup(
    name="pogym",
    version="0.0.1",
    description="A collection of partially-observable gym environments",
    python_requires=">=3.6",
    install_requires=["gym", "numpy"]
)
